# =============================================
# SKILLFIT ENVIRONMENT VARIABLES TEMPLATE
# =============================================
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your real secrets
# 3. NEVER commit .env to GitHub!
# =============================================

# ----- Flask Configuration -----
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=replace-with-a-long-random-secret
BASE_URL=http://localhost:5000

# ----- Database Configuration -----
# Use SQLite locally by leaving DATABASE_URL empty.
# On Render/Postgres, set full connection string.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_DB_HOST:5432/YOUR_DB_NAME
DB_SSLMODE=require
SQLALCHEMY_POOL_SIZE=5
SQLALCHEMY_MAX_OVERFLOW=5
SQLALCHEMY_POOL_RECYCLE=300

# ----- Admin Configuration -----
ADMIN_ID=admin@example.com
ADMIN_PASSWORD=change-this-password
ADMIN_CREDENTIALS=

# ----- Feature Flags -----
ROADMAP_SUPPORT=true
ML_CLASSIFIER_ENABLED=false
GITHUB_INTEGRATION_ENABLED=false

# ----- Job Service API Keys -----
ADZUNA_APP_ID=your-adzuna-app-id
ADZUNA_API_KEY=your-adzuna-api-key
RAPIDAPI_KEY=your-rapidapi-key

# ----- Optional API Keys -----
OPENAI_API_KEY=your-openai-api-key
GITHUB_API_TOKEN=your-github-token

# ----- GitHub OAuth -----
GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret
OAUTH_REDIRECT_URL=http://localhost:5000/auth/github/callback

# ----- Mail Service -----
MAIL_SERVER=smtp.your-provider.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-smtp-username
MAIL_PASSWORD=your-smtp-password
MAIL_DEFAULT_SENDER=no-reply@your-domain.com
SMTP_TIMEOUT_SECONDS=8
PASSWORD_RESET_COOLDOWN_SECONDS=300
# Set to false to disable email sending for testing or when SMTP is not configured
ENABLE_EMAIL_SENDING=true
